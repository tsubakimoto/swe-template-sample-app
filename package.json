{
  "name": "swe-template-sample-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 8000",
    "start": "npm run dev",
    "build": "vite build",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.3",
    "jsdom": "^24.1.1",
    "vite": "^5.4.10",
    "vitest": "^2.1.3"
  }
}
