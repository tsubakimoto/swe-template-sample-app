{
  "name": "swe-template-sample-app",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JavaScript Tic Tac Toe",
  "scripts": {
    "dev": "http-server -p 8000 -c-1 .",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "echo \"No build step required\""
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.7.0"
  }
}
